import 'package:flutter/material.dart';

class Items {
  final String? title;
  final String? date;
  final String? url;
  final String? link;

  const Items({Key? key, this.title, this.date, this.url, this.link});
}
